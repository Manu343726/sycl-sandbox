#pragma once
// Forwarding header — the canonical definitions now live in
// <sycl-sandbox/scene/data.h>: SceneView (non-owning view), SceneData
// (owning buffers), SceneBuilder, Handle dispatch, BVH.
// Kept for backward compatibility with old includes.
#include <sycl-sandbox/scene/data.h>
